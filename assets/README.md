# Plugin assets

- **icon.png** — Optional menu icon for the Bridge Builder sidebar (recommended 20×20 or 36×36 px).  
  If present, it is used as the admin menu icon; otherwise the plugin uses a dashicon.
